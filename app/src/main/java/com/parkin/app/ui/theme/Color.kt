package com.parkin.app.ui.theme

import androidx.compose.ui.graphics.Color

// Tons de Branco e Cinza (Light Mode)
val SurfaceWhite = Color(0xFFFFFFFF)  // Branco puro
val LightBg = Color(0xFFF8F9FA)       // Cinza muito claro para o fundo
val BorderGray = Color(0xFFE9ECEF)    // Cinza para as bordas dos inputs

// Tons de Texto
val TextPrimary = Color(0xFF212529)   // Quase preto (muito legível)
val TextSecondary = Color(0xFF6C757D) // Cinza para textos menores

// Cores de Destaque (Brand Colors)
val AccentBlue = Color(0xFF1E56A0)    // O azul para os botões e ícones
